#pragma once

// ============================================================================
// HWID-лицензия чита.
//
// По умолчанию проверка ВЫКЛЮЧЕНА: DLL запускается на любом ПК.
// Чтобы включить привязку к железу (приватная сборка), сделайте ОДНО из двух:
//   1) раскомментируйте строку ниже, либо
//   2) добавьте PRIVATE_BUILD в Preprocessor Definitions проекта (/DPRIVATE_BUILD).
//
// Белый список ключей: License.cpp -> AllowedKeys.
// ============================================================================

// #define PRIVATE_BUILD

namespace License
{
	// Генерирует HWID-ключ текущего ПК.
	// Формула сохранена 1:1 с оригиналом, старые ключи продолжают работать.
	extern int GenerateKey();

	// Проверяет, есть ли ключ в белом списке.
	extern bool IsAllowed( int key );

	// GenerateKey() + IsAllowed() одним вызовом.
	extern bool Check();
}
